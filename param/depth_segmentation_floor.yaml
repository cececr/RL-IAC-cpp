depth_segmentation: { intDebug: 1,
    floor_equation_filename: "$PROJECT_PATH/data/floor.dat",
    ransac_floor_dist_thresh: 0.05,
    normal_estim_max_depth: 0.02,
    normal_estim_smooth_size: 10.0,
    max_depth_visibility: 4.0,
    max_obj_dist_to_floor: 2.0,
    min_obj_dist_to_floor: 0.03,
    floor_angular_tolerance: 0.9,
    ransac_wall_dist_thresh: 0.1,
    min_wall_diag_size: 1.5,
    wall_angular_tolerance: 0.2,
    min_dist_btw_obj: 0.06,
    obj_min_diag_size: 0.2,
    obj_max_diag_size: 1.3,
    obj_at_border_pix_tolerance: 30,
    voxel_leaf_size: 0.03,
    max_obj_bottom_dist_to_floor: 1.3,
    min_obj_pixel_size: 200,
    use_tracking_option: 0,
    merge_clusters_option: 1,
    wrong_floor_thresh: 0.3
    }
